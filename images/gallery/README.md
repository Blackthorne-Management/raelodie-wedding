Drop real photos here (e.g. `first-date.jpg`), then in `gallery.html` replace a placeholder `<figure>` with:

```html
<figure><img src="images/gallery/first-date.jpg" alt="Describe the moment"></figure>
```
